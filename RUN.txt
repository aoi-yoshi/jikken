Thesis local pipeline (Qwen2.5-VL-3B) — project root: c:\Python\実験_修正

This project uses conda env `flvl` (Python 3.10) per the experiment plan.
Env path: C:\Users\aoi7y\miniconda3\envs\flvl

0) One-time setup (already done)
   - Miniconda installed via winget (Anaconda.Miniconda3).
   - conda env created with conda-forge only:
       conda create -y -n flvl -c conda-forge --override-channels python=3.10
       conda install -y -n flvl -c conda-forge --override-channels pip
   - PyTorch (Blackwell sm_120 / RTX 5050 needs cu128):
       pip install --index-url https://download.pytorch.org/whl/cu128 torch torchvision torchaudio
   - Other libs:
       pip install transformers accelerate peft datasets safetensors PyYAML tqdm \
                   scikit-learn pandas "fsspec>=2023.1.0,<=2026.2.0" flwr protobuf opencv-python

   avalanche-lib is intentionally NOT installed yet. On Windows it pulls qpsolvers/proxsuite
   which requires CMake+MSVC to build from source. We add it later when Step 6 (continual
   learning) is actually needed. The local Step 6 logic (forgetting log) does not require it.

1) Always run scripts with the flvl python (full path is safest)
   PY=C:\Users\aoi7y\miniconda3\envs\flvl\python.exe
   & $PY scripts\step00_env_check.py

2) Step0–2 (data under artifacts/ and data/)
   & $PY scripts\step00_env_check.py
   & $PY scripts\step01_qwen_infer.py
   & $PY scripts\step02_nexar_or_synthetic.py
   Put real NEXAR mp4 files under data/nexar_raw/videos/ to override synthetic videos.

3) Step3–4 (single-GPU training)
   & $PY scripts\step03_baseline_probe.py
   & $PY scripts\step04_lora_train.py

4) Step5 (Flower FL on one PC)
   default.yaml: fl.gpu_serialize=true (1 GPU なら必須。client 0/1 が GPU を順番使用)
   マルチ PC（各 client が別 GPU）なら fl.gpu_serialize=false

   Terminal A (server):
   C:\Users\aoi7y\miniconda3\envs\flvl\python.exe scripts\step05_flower_server.py

   Terminal B (client 0):
   $env:THESIS_CLIENT_ID="0"
   C:\Users\aoi7y\miniconda3\envs\flvl\python.exe scripts\step05_flower_client.py

   Terminal C (client 1; gpu_serialize 有効なら GPU のままで可):
   $env:THESIS_CLIENT_ID="1"
   C:\Users\aoi7y\miniconda3\envs\flvl\python.exe scripts\step05_flower_client.py

   run_id は省略可（サーバが LATEST_RUN_ID に書き、client が読む）

   Post-FL eval of aggregated LoRA:
   & $PY scripts\step05_fl_post_eval.py --run-id $RUN_ID

   Minimal metrics test (1 sample/client, 2 clients, 1 round; local FedAvg):
   & $PY scripts\run_fl_minimal.py

   Automated smoke E2E (1 round, 40 samples):
   & $PY scripts\run_fl_e2e_smoke.py

   Logs: artifacts\runs\step05_fl\<run_id>\
   Global checkpoint: artifacts\checkpoints\step05_fl\<run_id>\step05_fedavg_global.pt
   Latest pointer:     artifacts\checkpoints\step05_fl\LATEST_CHECKPOINT
   FL details: docs\fl_experiments.md

   Preview partitions (optional):
   & $PY scripts\fl_prepare_partitions.py --run-id my_run
   Non-IID example:
   & $PY scripts\fl_prepare_partitions.py --run-id my_run --partition-mode label_skew_dirichlet --label-skew-alpha 0.2
   Scene skew: --partition-mode key_skew  (key_field: scene in default.yaml)

5) Step6 (continual learning / forgetting; pure PyTorch, Avalanche later)
   教授指示により一時スキップ。Step 7 を優先。
   & $PY scripts\step06_continual_forgetting.py

6) Step7 Adaptation Consistency（プロトタイプ）
   1-batch 診断（Lpred/Lgrad/Lpost 計算確認）:
   & $PY scripts\step07_loss_probe.py

   スモーク（10 step）:
   & $PY scripts\step07_ablation_modes.py --mode lpred --max-steps 10 --max-train-samples 20 --eval-max 4 --run-suffix _smoke
   & $PY scripts\step07_ablation_modes.py --mode lpred_grad --max-steps 10 --max-train-samples 20 --eval-max 4 --run-suffix _smoke
   & $PY scripts\step07_ablation_modes.py --mode lpred_post --max-steps 10 --max-train-samples 20 --eval-max 4 --run-suffix _smoke

   短縮本番（100 step、Step 4b/5 同等データ規模）:
   & $PY scripts\step07_ablation_modes.py --mode lpred --max-steps 100 --run-suffix _short
   & $PY scripts\step07_ablation_modes.py --mode lpred_grad --max-steps 100 --run-suffix _short
   & $PY scripts\step07_ablation_modes.py --mode lpred_post --max-steps 100 --run-suffix _short

   本番 ablation（全 train 行 × epoch）:
   & $PY scripts\step07_ablation_modes.py --mode lpred --epochs 1
   & $PY scripts\step07_ablation_modes.py --mode lpred_grad --epochs 1
   & $PY scripts\step07_ablation_modes.py --mode lpred_post --epochs 1

   Logs: artifacts\runs\step07_ablation_<mode>\<run_id>\

7) Step5 + Step7 FL 統合（fl.consistency_mode）
   default.yaml: fl.consistency_mode = none | lpred | lpred_grad | lpred_post
   または CLI: --consistency-mode lpred

   Terminal A (server):
   & $PY scripts\step05_flower_server.py

   Terminal B/C (clients):
   $env:THESIS_CLIENT_ID="0"
   & $PY scripts\step05_flower_client.py --consistency-mode lpred

   AC FL スモーク（1 round, 小データ）:
   & $PY scripts\run_step07_fl_smoke.py --consistency-mode lpred

Notes
- HF model cache: default at $env:USERPROFILE\.cache\huggingface (override with $env:HF_HOME).
- Config: config\default.yaml のみ（LoRA 比較もここを一時編集）。
- GPU 計測: src\resource_metrics.py（実験時は自動ログ）。手動確認は step00 または python -m src.resource_metrics

Where to look (迷ったらここ)
- docs 目次 .......... docs\README.md
- 設定 .............. config\default.yaml
- runs の読み方 ..... docs\runs_layout.md
- 進捗・ファイル方針 . docs\code_organization.md
- Step 4b 結果 ...... artifacts\runs\step04b_diff_probe\<run_id>\summary.json, epoch_history.jsonl
- Step 5 FL .......... artifacts\runs\step05_fl\<run_id>\
- グローバル LoRA .... artifacts\checkpoints\step05_fl\<run_id>\（LATEST_CHECKPOINT で最新パス）
- 構成マップ .......... docs\project_map.md
- Step5 進捗会資料 .... docs\step05_flower_report.md
- FL 手順 ............. docs\fl_experiments.md
- GPU 計測コード .... src\resource_metrics.py（1 ファイルに集約）

- The earlier .venv\ in this folder is no longer used. You may delete it:
    Remove-Item -Recurse -Force .\.venv

GPU sanity
- & $PY -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"
- Expected: True NVIDIA GeForce RTX 5050 Laptop GPU
