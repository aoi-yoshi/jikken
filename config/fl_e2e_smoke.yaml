# Flower E2E スモークテスト用（1 round, 小データ, 短時間確認）
# 本番 FL 前のパイプライン動作確認に使用。進捗会デモ後は config/default.yaml に戻す。

train:
  eval_max: 10

data:
  max_train_samples: 8

fl:
  num_rounds: 1
  local_epochs: 1
  min_fit_clients: 2
  min_available_clients: 2
